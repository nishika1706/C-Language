#include <stdio.h>
int main()
{ 
float radius,area;
const float PI=3.14159;
printf("enter radiusof circle:");
scanf("%f",&radius);
Area=PI*radius*radius;
printf("Area of circle=%.2f\n",area);
return 0;
}
